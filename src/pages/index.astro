---
import ListCategories from '@/components/ListCategories'
import ListPosts from '@/components/ListPosts'
import TitlePage from '@/components/TitlePage'
import BaseLayout from '@/layouts/BaseLayout'
import Hero from '@/components/Hero'
import CarruselImgs from '@/components/CarruselImgs'
import { getPosts } from '@/utils'

const MAX_POSTS = 40 // max number of posts to show on the home page
const posts = await getPosts(MAX_POSTS)
---

<BaseLayout title='Inicio'>
	<Hero />
	<CarruselImgs />
	<TitlePage title='Todos Nuestros modelos' />
	<ListCategories />

	<div>
		<h3 class='text-lg font-medium tracking-wide text-end'>Últimos modelos</h3>
		<ListPosts FirstBig={true} posts={posts} />
	</div>
</BaseLayout>

<script>
	import { animate } from 'motion'
	const showAnimations = localStorage.getItem('animations') === 'true'

	if (showAnimations) {
		animate(
			'.title',
			{ y: [80, 0], opacity: [0, 1] },
			{
				duration: 2.5,
				opacity: { duration: 3 },

				offset: [0, 0.55, 0.75]
			}
		)
	}
</script>
